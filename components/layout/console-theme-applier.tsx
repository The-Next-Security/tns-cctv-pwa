'use client'

// Sistema de consola reemplazado por ThemeToggle (bm-design-system).
// Este archivo se mantiene vacío para no romper imports existentes.

export function ConsoleThemeApplier() {
  return null
}

export function setConsoleThemeManual(_theme: string, _setTheme: (t: string) => void) {
  // No-op
}
